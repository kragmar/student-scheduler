import { CurriculumTitlePipe } from './curriculum-title.pipe';

describe('CurriculumTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new CurriculumTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
